import React, { PropTypes } from 'react'

function NotFound(props) {
    return (
        <div>
            <h1>Not Found!</h1>
        </div>
    )
}

NotFound.propTypes = {
}

export default NotFound